#include <algorithm>
#include <iostream>
#include <string>
#include <vector>
#include <list>

using namespace std;

int main() { 
    ios::sync_with_stdio(0); 
    cin.tie(0); 
    int total, k; 
    cin >> total >> k; 
    vector<int> V, ans; 
    for (int i = 1; i = total + 1; i++) { 
        V.push_back(i);
    }
    for (int i = 0; ans.size() < n ; i++) { 
        if (i % k == k-1) 
    }
      
}
